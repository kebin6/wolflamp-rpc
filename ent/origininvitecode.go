// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/kebin6/wolflamp-rpc/ent/origininvitecode"
)

// 原始邀请码管理表
type OriginInviteCode struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// Create Time | 创建日期
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Update Time | 修改日期
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Status 1: normal 2: ban | 状态 1 正常 2 禁用
	Status uint8 `json:"status,omitempty"`
	// 邀请码
	Code         string `json:"code,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*OriginInviteCode) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case origininvitecode.FieldID, origininvitecode.FieldStatus:
			values[i] = new(sql.NullInt64)
		case origininvitecode.FieldCode:
			values[i] = new(sql.NullString)
		case origininvitecode.FieldCreatedAt, origininvitecode.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the OriginInviteCode fields.
func (oic *OriginInviteCode) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case origininvitecode.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			oic.ID = uint64(value.Int64)
		case origininvitecode.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				oic.CreatedAt = value.Time
			}
		case origininvitecode.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				oic.UpdatedAt = value.Time
			}
		case origininvitecode.FieldStatus:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				oic.Status = uint8(value.Int64)
			}
		case origininvitecode.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				oic.Code = value.String
			}
		default:
			oic.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the OriginInviteCode.
// This includes values selected through modifiers, order, etc.
func (oic *OriginInviteCode) Value(name string) (ent.Value, error) {
	return oic.selectValues.Get(name)
}

// Update returns a builder for updating this OriginInviteCode.
// Note that you need to call OriginInviteCode.Unwrap() before calling this method if this OriginInviteCode
// was returned from a transaction, and the transaction was committed or rolled back.
func (oic *OriginInviteCode) Update() *OriginInviteCodeUpdateOne {
	return NewOriginInviteCodeClient(oic.config).UpdateOne(oic)
}

// Unwrap unwraps the OriginInviteCode entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (oic *OriginInviteCode) Unwrap() *OriginInviteCode {
	_tx, ok := oic.config.driver.(*txDriver)
	if !ok {
		panic("ent: OriginInviteCode is not a transactional entity")
	}
	oic.config.driver = _tx.drv
	return oic
}

// String implements the fmt.Stringer.
func (oic *OriginInviteCode) String() string {
	var builder strings.Builder
	builder.WriteString("OriginInviteCode(")
	builder.WriteString(fmt.Sprintf("id=%v, ", oic.ID))
	builder.WriteString("created_at=")
	builder.WriteString(oic.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(oic.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", oic.Status))
	builder.WriteString(", ")
	builder.WriteString("code=")
	builder.WriteString(oic.Code)
	builder.WriteByte(')')
	return builder.String()
}

// OriginInviteCodes is a parsable slice of OriginInviteCode.
type OriginInviteCodes []*OriginInviteCode
